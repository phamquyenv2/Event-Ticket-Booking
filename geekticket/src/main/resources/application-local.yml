spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/geekticketdb
    username: geekticket
    password: '123456'

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.quyen.geekticket: DEBUG
